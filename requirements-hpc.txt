# Use command-line index options with uv pip install (do not put them in this file)
## uv pip install --upgrade --reinstall \
#  --index-strategy unsafe-best-match \
#  --extra-index-url https://download.pytorch.org/whl/cu121 \
#  -r requirements-hpc.txt

# Core training/evaluation stack (pinned to versions already working in Flower container runtime)
torch==2.5.1
torchaudio==2.5.1
transformers==5.6.2
tokenizers==0.22.2
peft==0.19.1
datasets==4.8.5
pyarrow>=21.0.0
accelerate==1.13.0
huggingface-hub>=0.34.0
jiwer>=4.0.0
numpy==2.3.5
pandas==3.0.2
tqdm<=4.66.5
sentencepiece==0.2.1
soundfile==0.13.1
tensorboard>=2.20.0
silero-vad>=6.2.1
tiktoken>=0.12.0

# Notebook/analysis extras
matplotlib>=3.10.0
ipywidgets>=8.1.0
librosa>=0.10.2

# Optional text normalization helpers used by Voxtral normalization paths
num2words>=0.5.14
pyahocorasick>=2.3.0
